export enum SortingOptions {
  SEQUENCE,
  ALPHA,
}

export enum RuneNewFormLocations {
  START,
  END,
}

export enum EditStates {
  IDLE,
  ADDING_RUNE,
  EDITING_RUNE,
  ADDING_CHAIN,
  EDITING_CHAIN,
}
